server:
  port: 8080

spring:
  datasource:
    name: hibernate_jdbc
    url: jdbc:mysql://localhost:3306/hibernate_jdbc?serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

